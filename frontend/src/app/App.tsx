export function App() {
  return (
    <main className="app-shell">
      <section className="hero-card" aria-labelledby="app-title">
        <p className="eyebrow">MyNovel Frontend</p>
        <h1 id="app-title">React workspace ready</h1>
        <p className="lede">
          This Vite skeleton is ready for the upcoming SPA migration.
        </p>
      </section>
    </main>
  );
}
